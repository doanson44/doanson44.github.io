name: CI

# Temporarily disabled: validation is intentionally skipped while the project is under active development.
# Keep this workflow file so the quality gates can be restored later without rebuilding the CI configuration.

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    name: Validate Rust WASM application
    runs-on: ubuntu-latest
    steps:
      - name: CI disabled
        run: echo "CI validation is temporarily disabled. Use the deploy workflow for master deployments."
