name: Deploy to GitHub Pages

on:
  push:
    branches:
      - master
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    if: github.ref == 'refs/heads/master'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown
          components: rustfmt, clippy

      - name: Rust cache
        uses: Swatinem/rust-cache@v2.9.2

      - name: Format Rust sources
        run: cargo fmt

      - name: Check formatting
        run: cargo fmt --check

      - name: Check compilation
        run: cargo check --target wasm32-unknown-unknown

      - name: Run tests
        run: cargo test

      - name: Run clippy
        run: cargo clippy --target wasm32-unknown-unknown -- -D warnings

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 24

      - name: Install Node dependencies
        run: npm install

      - name: Install Trunk
        run: |
          wget -qO- https://github.com/trunk-rs/trunk/releases/latest/download/trunk-x86_64-unknown-linux-gnu.tar.gz | tar -xzf-
          chmod +x trunk
          sudo mv trunk /usr/local/bin/

      - name: Build release
        run: |
          for attempt in 1 2 3; do
            if trunk build --release; then
              exit 0
            fi
            echo "Trunk build attempt $attempt failed; retrying..."
            sleep $((attempt * 10))
          done
          exit 1

      - name: Verify static assets
        run: |
          test -f dist/favicon.svg
          test -f dist/index.html
          test -n "$(find dist -maxdepth 1 -type f -name 'tailwind.generated-*.css' -print -quit)"

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: ./dist

  deploy:
    if: github.ref == 'refs/heads/master'
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      # Force a fresh build/deploy run for runtime verification.
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
